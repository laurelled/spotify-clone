import { SidebarButtonDirective } from './sidebar-button.directive';

describe('SidebarButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarButtonDirective();
    expect(directive).toBeTruthy();
  });
});
